- name: Create GCP VPC
  hosts: localhost
  vars:
    gcp_project:
    gcp_cred_kind: serviceaccount
  tasks:
    - name: Create VPC
      gcp_compute_network:
        name: test-vpc
        project: "{{ gcp_project }}"
        auth_kind: "{{ gcp_cred_kind }}"
        state: absent
      register: network
