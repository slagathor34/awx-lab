---
- hosts: localhost
  connection: local

  tasks:
  - name: Delete a Resource Group
    azure_rm_resourcegroup:
      name: "{{ rg_name }}"
      location: westus
      state: absent
      tags:
          testing: testing
          delete: never
